# Optional — cloud deploy. Primary deploy: docs/DEPLOY-VPS.md (Docker on VPS).
# Render Blueprint — Haulyard API (monorepo root)
# Docs: docs/DEPLOY-VERCEL-RENDER.md
#
# After deploy: set FRONTEND_URL + CROSS_SITE_COOKIES=true + S3 + Upstash REDIS_URL in dashboard.

services:
  - type: web
    name: haulyard-api
    runtime: node
    plan: free
    region: oregon
    buildCommand: >-
      corepack enable &&
      corepack prepare pnpm@9.15.0 --activate &&
      pnpm install --frozen-lockfile &&
      pnpm --filter @haulyard/shared-types build &&
      pnpm --filter @haulyard/api exec prisma generate &&
      pnpm --filter @haulyard/api build
    startCommand: pnpm start:api
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
      - key: CROSS_SITE_COOKIES
        value: "true"
      - key: DATABASE_URL
        fromDatabase:
          name: haulyard-db
          property: connectionString
      - key: JWT_ACCESS_SECRET
        generateValue: true
      - key: JWT_REFRESH_SECRET
        generateValue: true
      - key: FRONTEND_URL
        sync: false
      - key: REDIS_URL
        sync: false
      - key: AWS_ACCESS_KEY_ID
        sync: false
      - key: AWS_SECRET_ACCESS_KEY
        sync: false
      - key: AWS_REGION
        value: auto
      - key: S3_BUCKET_NAME
        sync: false

databases:
  - name: haulyard-db
    plan: free
    databaseName: haulyard
    user: haulyard
